PARAMETERS = {
    "file": "input_rf_genes_count_mut",  # Name of file to use as input data
    "labels": "M_STAGE",  # Label of labels to classify
    "columns_to_drop": ["M_STAGE"],  # Columns to drop on input
    "name": "count_mut_oversample",  # Name to be used to save results
    "option": "over"  # split, under, over, cv
}
